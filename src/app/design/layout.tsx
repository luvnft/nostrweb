export const metadata = {
  title: "Web.atl5d | Design",
  description: "",
};

export default async function DesignLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
