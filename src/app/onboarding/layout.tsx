export const metadata = {
  title: "Web.atl5d | Onboarding",
  description: "",
};

export default async function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
